/**
 * The nine-screen app flow. See `screens.ts` for why the visual half of the
 * spec is deliberately absent from this module.
 */
export {
  INITIAL_PROGRESS,
  eventsFor,
  initialState,
  reduce,
  resumeScreen,
  type FlowAction,
  type FlowProgress,
  type FlowState,
  type ScreenId,
} from './screens';
